services:
  playwright-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    ports:
      - "8931:8931"
    restart: unless-stopped
    # Increase shared memory for Chrome stability
    shm_size: "1gb"
